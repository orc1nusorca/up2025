<Page x:Class="PM_04_ISP32_SSE.pages.MaterialList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PM_04_ISP32_SSE.pages"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1200"
      Title="MaterialList" Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="450"></ColumnDefinition>
                <ColumnDefinition Width="450"></ColumnDefinition>
                <ColumnDefinition Width="270"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Border>
                    <TextBox Name="TBoxSearch" Width="410" TextChanged="TBoxSearch_TextChanged"/>
                </Border>
            </Grid>
            <Grid Grid.Column="1">
                <Border>
                    <ComboBox HorizontalAlignment="Right" Width="200" Name="ComboSortBy" SelectionChanged="ComboSortBy_SelectionChanged">
                        <ComboBoxItem Content="Сортировка" VerticalAlignment="Center"></ComboBoxItem>
                        <ComboBoxItem Content="Наименование (А-Я)" VerticalAlignment="Center"></ComboBoxItem>
                        <ComboBoxItem Content="Наименование (Я-А)" VerticalAlignment="Center"></ComboBoxItem>
                        <ComboBoxItem Content="Остаток на складе (возр.)" VerticalAlignment="Center"></ComboBoxItem>
                        <ComboBoxItem Content="Остаток на складе (убыв.)" VerticalAlignment="Center"></ComboBoxItem>
                        <ComboBoxItem Content="Стоимость (возр.)" VerticalAlignment="Center"></ComboBoxItem>
                        <ComboBoxItem Content="Стоимость (убыв.)" VerticalAlignment="Center"></ComboBoxItem>
                    </ComboBox>
                </Border>
            </Grid>
            <Grid Grid.Column="2">
                <Border>
                    <ComboBox HorizontalAlignment="Right" Width="200" Name="ComboFilterBy" SelectionChanged="ComboSortBy_SelectionChanged"></ComboBox>
                </Border>
            </Grid>
        </Grid>
        <Grid Grid.Row="1">
            <TextBlock Name="TxtRecordCount" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20, 0"/>
        </Grid>
        <ListView Grid.Row="2" Name="LViewServices" SelectionMode="Multiple" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="Transparent" BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="{Binding RowBackground}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"></WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border>
                        <Grid MinWidth="1200">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0" Width="200" Height="200" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <Image Source="{Binding MaterialImage}"
                                   Width="200"
                                   Height="200"
                                   Stretch="UniformToFill">
                                </Image>
                            </Grid>
                            <StackPanel Grid.Column="1" Margin="0, 10" Orientation="Horizontal">
                                <StackPanel Orientation="Vertical" MaxWidth="650">
                                    <TextBlock Text="{Binding DisplayNameMaterial}" HorizontalAlignment="Left" Margin="0, 3"/>
                                    <TextBlock Text="{Binding DisplayOstatok}" HorizontalAlignment="Left" Margin="0, 3"/>
                                    <TextBlock Text="{Binding DisplayCena}" HorizontalAlignment="Left" Margin="0, 3"/>
                                    <TextBlock Text="{Binding DisplayType}" HorizontalAlignment="Left" Margin="0, 3"/>
                                    <TextBlock Text="{Binding DisplayOpisanie}" HorizontalAlignment="Left" Margin="0, 3" TextWrapping="Wrap"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Row="3">
            <StackPanel Orientation="Horizontal">
                <Button Name="BtnAddMaterial" Click="AddMaterialButton_Click" Content="Добавить материал" Margin="10"></Button>
                <Button Name="BtnEditMaterial" Content="Редактировать" Click="EditMaterial_Click" Margin="10" Visibility="Collapsed"/>
                <Button Name="BtnDeleteMaterial" Content="Удалить" Margin="10" Background="#FFE74C3C" Foreground="White" Click="DeleteButton_Click" Visibility="Collapsed"/>
                <Button Name="BtnChangeMinQuantity" Content="Изменить мин. количество" HorizontalAlignment="Right" Margin="10" Click="BtnChangeMinQuantity_Click" Visibility="Collapsed"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10, 0">
                <Button Name="BtnPreviousPage" Content="&lt;" Margin="5" Width="30" Click="BtnPreviousPage_Click"/>

                <ItemsControl Name="PageNumbersControl" ItemsSource="{Binding PageNumbers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}" Margin="2" Width="30" Click="PageNumberButton_Click"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Name="BtnNextPage" Content="&gt;" Margin="5" Width="30" Click="BtnNextPage_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
